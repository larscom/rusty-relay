# 🦀 Rusty Relay Messages

<p align="center">
  <strong>Shared message structures for the Rusty Relay project.</strong>
</p>

---

## 🚀 Overview

`relay-messages` is a library crate that contains the shared message definitions used for communication between the `relay-server` and `relay-client`. This ensures that both the client and server can serialize and deserialize messages in a consistent way.

## 📦 Crate

This crate is part of the [Rusty Relay](https://github.com/larscom/rusty-relay) workspace. It is used as a dependency by the `relay-server` and `relay-client` crates.
